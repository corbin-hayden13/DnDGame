class Weapon:
    def __init__(self, name, damage, dmg_type, cost):
        self.name = str(name)
        self.damage = int(damage)
        self.dmg_type = str(dmg_type)
        self.cost = int(cost)

